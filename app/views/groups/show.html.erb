<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: @group.owner_user %>

      <h2 class="mt-3">New book</h2>
      <%= render 'books/form', book: @book %>
      </div>
      <div class='col-md-8 offset-md-1'>
        <h2>Groups Detail</h2>
        <table class='table'>
          <tbody>
              <tr>
                <td><%= image_tag @group.get_image, size: '50x50' %></td>
                <td>
                  <%= @group.group_name %>
                </td>
                <td><%= @group.introduction %></td>
                <td>
                  <% if current_user==@group.owner_user %>
                    <%= link_to "Edit",edit_group_path(@group.id),method: :get ,class: "btn btn-success" %>
                  <% else %>
                    <% if @group.group_mambers.include?(current_user) %>
                      <%= link_to 
                    <% end %>
                  <% end %>
                </td>
              </tr>
          </tbody>
      </table>
    </div>
  </div>
</div>
